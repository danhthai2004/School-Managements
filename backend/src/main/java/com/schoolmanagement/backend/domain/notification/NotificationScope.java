package com.schoolmanagement.backend.domain.notification;

/**
 * Scope for system notifications.
 */
public enum NotificationScope {
    ALL, // All users in the system
    SCHOOL, // All users in a specific school
    ROLE // All users with a specific role
}
